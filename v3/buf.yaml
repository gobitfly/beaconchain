version: v2
modules:
  - path: api/protos
deps:
  # Always run `buf dep update` after updating this
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
  - buf.build/bufbuild/protovalidate:v0.14.1
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
