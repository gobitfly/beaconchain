export default defineNuxtRouteMiddleware(async () => {
  // const { isLoggedIn } = useUser()
  // if (!isLoggedIn) return
  // await useApi('/api/bff/users/me/dashboards', {
  //   getCachedData: (key, nuxtApp) => nuxtApp.payload[key] ?? nuxtApp.payload.data[key],
  // })
  // const { validatorDashboards } = usePrivateDashboards()
  // if (validatorDashboards.value.length) {
  //   return await navigateTo({
  //     name: 'dashboard-id',
  //     params: {
  //       id: validatorDashboards.value[0].id,
  //     },
  //   })
  // }
})
